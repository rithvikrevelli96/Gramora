<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gramora.app">

    <!-- ✅ Internet permission -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- ✅ Vibration permission -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:label="Gramora"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/Theme.Gramora"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true">

        <!-- 🟣 Welcome Screen: Launcher -->
        <activity
            android:name=".WelcomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login Screen -->
        <activity android:name=".LoginActivity" android:exported="true" />

        <!-- 🟢 Signup Screen -->
        <activity android:name=".SignupActivity" android:exported="true" />

        <!-- 🟠 Dashboard Screen -->
        <activity android:name=".DashboardActivity" android:exported="true" />

        <!-- 🟡 Content Creation Screen -->
        <activity android:name=".ContentCreationActivity" android:exported="true" />

        <!-- ⚙️ Optional: MainActivity -->
        <activity android:name=".MainActivity" android:exported="true" />

        <!-- 🟤 Auth Screen (added) -->
        <activity android:name=".AuthActivity" android:exported="true" />

    </application>
</manifest>